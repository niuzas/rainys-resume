import React from 'react';

import * as S from './SectionHeader.style';

const SectionHeader = ({ sectionTitle }) => <S.SectionHeaderBlock>{sectionTitle}</S.SectionHeaderBlock>;

export default SectionHeader;
